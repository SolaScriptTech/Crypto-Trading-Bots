# Security Policy

Report vulnerabilities through GitHub's private security-advisory feature.

Never post API credentials, passwords, session tokens, account identifiers, state files, audit trails, trade databases, infrastructure addresses, private-key paths, or logs in a public issue.

If a credential is ever committed, revoke it immediately. Removing it from the current branch does not remove it from Git history.
